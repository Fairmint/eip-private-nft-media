{
  "buildCommand": "npm run build && npm run demo:build",
  "functions": {
    "api/**/*.ts": {
      "maxDuration": 10
    }
  },
  "installCommand": "npm ci",
  "outputDirectory": "demo/web/dist",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api"
    }
  ]
}
